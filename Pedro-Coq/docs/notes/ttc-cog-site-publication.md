# Pedro private-site publication

## Version 3 publication

Published on 2026-09-21, America/Chicago
(deployment timestamp `2026-09-22T04:13:50.708251+00:00`).

**Version 3 was published with owner-only access** at
[Pedro-Coq Research Notes](https://pedro-proof-notes.tra38.chatgpt.site).
The Sites service returned terminal deployment status `succeeded`.
The subsequent access read showed the current account as owner, a single
allowed user with role owner, zero workspace/tenant groups, and zero external
visitors. The existing custom owner allowlist was preserved; no access-change
operation was needed.

## Content and evidence

That version incorporates the work through proof-repository commit
`5973ff12123c298be4a158f2ea94aaae031fb8a9`:

- the bounded geometry search and failed ground-pound descent/successor;
- the US/JP 1,200-update STOPPED hold, distinct from RANDOM-mode RNG control;
- the preserving input comparisons whose ordered draws match the control;
- the observed RANDOM cog departure and actual interleaved cog schedule;
- the complete local US/JP Clight cog-departure result and its explicit premises;
- the source-level Bob-omb, cloned-coin and environmental-effect findings.

The site and guide agree that a reachable 1,200-frame RANDOM-mode strategy
using preserving RNG control remains unproved. Failed samples are not a
universal impossibility theorem. The scope remains `VERSION_US` and
`VERSION_JP`. This publication adds no proof theorem, experiment or new
gameplay claim, and does not change the SSL atlas or Pyramid Fine Print site.

Site source commit: `40ff1e8eb28a5390fa7a4c891fed5e1aaba9ce66`, pushed to the
existing Sites source repository before saving version 3. The saved version
reports that exact source commit and its successful deployment. The archive
was produced by the official Sites workflow from the clean source state.

All eight pinned proof-source links resolve to files in commit `5973ff1`.
Internal anchors and local asset references were checked. The four JSON
downloads parse and match their local proof-repository records byte for byte;
their contents also match the pinned Git records after line-ending
normalization. They contain experiment receipts, not ROMs or conversation
exports. The existing earlier ground-pound receipt remains available.

## Why the earlier publication blocker was recoverable

The Sites skill bundle was absent from the local skill directories but was
available through the connected MCP resource catalog. The catalog returned
the Sites plugin's bundle resource, which supplied the official archive.
Version 0.1.70 includes `skills/sites-building/SKILL.md`,
`skills/sites-hosting/SKILL.md` and `scripts/site-workflow.mjs`. Those skills
were read and their existing-site opening, push, packaging and private
deployment workflow was used. A missing local directory is therefore not
sufficient evidence that this bundle is unavailable.

For future publication, rediscover the Sites bundle through its resource
metadata if needed; use the returned resource identifier and download URL
unchanged. Keep the existing `.openai/hosting.json` project ID. Never record
short-lived repository credentials or signed download URLs in source.

On this Windows checkout, the helper needed a process-local Git trust entry
for this exact Site repository because the sandbox and user have different
file ownership. Packaging used Git Bash and tar's `--force-local` option so
the Windows drive prefix was treated as a local archive path. These process
settings did not change global Git configuration or the published audience.

## Version 4 publication

**Version 4 is live and remains owner-only**, published on 2026-09-22,
America/Chicago (deployment timestamp `2026-09-23T02:19:30.737247+00:00`).
The [video argument section](https://pedro-proof-notes.tra38.chatgpt.site/#video-argument)
now explains the video's scheduling warning, the exhaustive scalar RNG
checks, the exact prescribed-schedule seed filter and their limits. The site
and guide both distinguish finite arithmetic checks from a reachable,
preserving 1,200-frame RANDOM-mode gameplay witness, which remains unproved.

Proof evidence snapshot: `dfeb2aa06d17122f43944549bf4241adf8b635c6`.
Site source commit: `dba971ab633886f6e6a019d879c0cfc9fcd28429`.
Both source states were pushed before publication. The official workflow
packaged the clean site source, and the private save-and-deploy operation
returned terminal status `succeeded`. The saved version reports that exact
site commit. A subsequent access read confirmed one allowed user with role
owner, no groups and zero external visitors; no access change was made.

All nine pinned proof-source links and local asset references were checked.
The fifth JSON download, the scalar RNG receipt, matches the proof record
byte for byte. This publication adds no Coq theorem and does not change the
SSL atlas or Pyramid Fine Print site.
